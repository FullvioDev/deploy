services:
  nginx:
    image: nginx:${TAG:-1.30-alpine}
    container_name: ${CNTNR:-nginx}
    ports:
      - "${HOST_PORT:-8080}:80"
    volumes:
      - ${NGINX_DIR}:/usr/share/nginx/html:ro
      - ${NGINX_CNF:-.}/nginx.conf:/etc/nginx/nginx.conf:ro
    restart: unless-stopped
